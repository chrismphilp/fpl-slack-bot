const {leagueRanking} = require("./league-rankings");
const {playerHistory} = require("./player-history");
const {gameweekFixtures} = require("./gameweek-fixtures");

module.exports = {
    leagueRanking,
    playerHistory,
    gameweekFixtures,
}
