const {leagueRanking} = require("./league-rankings");
const {playerHistory} = require("./player-history");
const {gameweekFixtures} = require("./gameweek-fixtures");
const {gameweekDreamTeam} = require("./gameweek-dream-team");
const {gameweekStats} = require("./gameweek-stats");

module.exports = {
    leagueRanking,
    playerHistory,
    gameweekFixtures,
    gameweekDreamTeam,
    gameweekStats,
}
